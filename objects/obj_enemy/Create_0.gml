hp = 30;
hit_by_melee = false;
vsp = 0;              // Vertical speed (for gravity)
facing = choose(-1, 1); // Random initial direction (optional)
move_speed = 1.5;     // Patrol speed
hp = 30;              // Example health (can adjust)
attack_range = 16; // Distance to check for attacking
attack_cooldown = 1000; // in milliseconds
last_attack_time = 0;
damage = 10;
timer = 0;
bullet_speed = 6; // or whatever speed you like

